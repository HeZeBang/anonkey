apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: misskey
  labels:
    app: postgres
spec:
  # Headless service for StatefulSet stable DNS
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
